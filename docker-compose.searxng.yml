services:
  searxng:
    image: searxng/searxng:latest
    container_name: design-canvas-searxng
    ports:
      - "8080:8080"
    volumes:
      - ./searxng:/etc/searxng:rw
    restart: unless-stopped
